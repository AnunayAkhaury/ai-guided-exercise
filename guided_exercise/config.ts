export const config = {
  sessionName: 'DemoSession',
  roleType: '1',
  sessionPassword: '',
  displayName: 'Ekaansh',
  sessionIdleTimeoutMins: 10,
  useClientJwt: true
};

// NOTE: For demo only. Do not store secrets in the client for production.

